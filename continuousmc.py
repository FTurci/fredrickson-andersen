# This is a draft of generalisation

class continuousMonteCarlo(object):
  def __init__(self,model)
    self.model = model

  def compute_transition_probabilities():
    """Get a list of transition probabilities from the current state to all of the other states"""
    self.probabilities = model.compute_transitions()
  def choose_state():
    
  def compute_dt()
    
